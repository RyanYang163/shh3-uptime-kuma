# 隐私政策 — Uptime Kuma

> 适用审核项：C3（隐私政策有无）、C4（完整性）、C5（可访问）、C6（数据收集一致性）、C7（用户数据权利）、C8（第三方 API 说明）

**生效日期**：2026-09-18　**最近更新**：2026-09-21
**适用版本**：1.0.006
**应用形态**：TOS 7 Docker 应用（Compose）

## 一、数据收集与处理

Uptime Kuma 是**自托管**的服务监控工具，本应用不向开发者收集任何数据。

- 应用主动向**管理员在 WebUI 中自行配置**的地址发起探测（HTTP(s) / TCP / Ping / DNS / 关键字等）
- 监控项配置、探测记录、状态页、告警渠道配置，全部保存在**本机挂载目录** `/Volume*/DockerAppData/shh3-uptime-kuma/data`
- 告警触发时，按管理员配置的渠道把告警内容发送给对应服务商
- **本封装不含任何统计、埋点、遥测或回传代码**，开发者不持有、不接触任何用户数据
- 卸载时若勾选「同时删除数据」，上述目录将被清除

## 二、数据保存期限

- 运行数据保存在本机挂载目录中，**不设自动过期**，保留时长由用户自行管理
- 用户在 WebUI 中删除对应监控项 / 状态页后即被清除
- 卸载应用并选择「同时删除数据」后即被清除
- 开发者侧**不存在**任何副本，故无需另行约定开发者侧的保存期限

## 三、第三方共享与数据流向（C8）

**开发者不与任何第三方共享数据**（因为开发者根本不接收数据）。

仅在**管理员启用告警通知**时，告警内容会被发送到管理员自行配置的渠道。常见类型：

| 类别 | 典型服务 | 数据去向 |
|------|---------|---------|
| 邮件 | 管理员配置的 SMTP 服务器 | 管理员所指定的邮件服务商 |
| 即时通讯 | Telegram / Discord / Slack / 企业微信 / 钉钉 等 | 对应服务商的服务器所在地 |
| Webhook | 管理员填写的任意 URL | 由管理员指定 |
| 短信 / 推送 | 管理员配置的服务商 | 对应服务商的服务器所在地 |

- **传输地域**：取决于管理员所配置的服务商所在地，开发者不参与、不中转、不可见
- 未启用任何通知渠道时，除「应用主动探测的被监控目标」外**不产生任何对外数据发送**
- 本应用不含广告、分析或用户画像类第三方 SDK

## 四、数据安全措施（本应用的实际机制）

本应用以 **Docker 容器**方式运行，安全机制来自 `docker-compose.yml`，**不涉及 systemd**：

- **非 root 运行**：容器以 `user: "1000:1000"` 运行，不以 root 身份执行
- **禁止提权**：`security_opt: no-new-privileges:true`
- **不使用特权模式**：无 `privileged: true`
- **不使用 host 网络**：走 bridge 网络 + 端口映射，仅对外暴露 `18803`
- **资源上限**：`cpus: "0.5"`、`memory: 512M`
- **镜像锁定**：`louislam/uptime-kuma:2.5.5-slim`（具体版本，不使用 `:latest`）
- 数据仅保存在本机文件系统，不经由开发者服务器中转

## 五、用户数据权利（C7）

用户可随时查阅、更正、删除自己的数据：

1. **查阅 / 更正**：通过应用自身的 WebUI 操作
2. **删除**：删除目录 `/Volume*/DockerAppData/shh3-uptime-kuma/`，或在 TOS 应用中心卸载时勾选「同时删除数据」
3. **完全移除**：卸载应用并确认删除上述数据目录，本机即不再保留任何本应用数据

## 六、联系方式

如对隐私政策有疑问，请通过上游项目仓库提交 issue：
<https://github.com/louislam/uptime-kuma/issues>

本封装仓库（TOS 平台集成部分）的问题请提交到：
<https://github.com/RyanYang163/shh3-uptime-kuma/issues>
